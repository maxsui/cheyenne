<% content_for :title, "Séance" %>

<div class="row">
	<div class="col-md-12">
		<p><%= l @sceance.begin %></p>
		<p><%= l @sceance.end %></p>
	</div>
</div>

<div class="row">
	<div class="col-md-12">
		<h3>Participants</h3>
		<% if @sceance.customers.present? %>
		<div class="table-responsive">
			<table class="table table-striped">
				<tbody>
					<% @sceance.sceance_customers.each do |sceance_customer| %>
					<tr>
						<td><%= sceance_customer.customer.name %></td>
						<td><%= sceance_customer.observables.evaluated.count %> / <%= sceance_customer.observables.count %></td>
						<td>
							<%= link_to sceance_sceance_customer_path(@sceance, sceance_customer) do %>
							<i class="fa fa-eye fa-fw"></i>
							<% end %>
							<%= link_to sceance_customer.project do %>
							<i class="fa fa-list fa-fw"></i>
							<% end %>
						</td>
					</tr>
					<% end %>
				</tbody>
			</table>
		</div>
		<% end %>
	</div>
</div>

<div class="row">
	<div class="col-md-12">
		<h3>Encadrants</h3>

		<ul>
			<% @sceance.users.each do |user| %>
			<li><%= user.name %></li>
			<% end %>
		</ul>
	</div>
</div>

<%= link_to 'Editer', edit_sceance_path(@sceance), class: "btn btn-primary" %>
<%= link_to 'Supprimer', sceance_path(@sceance), class: "btn btn-default", method: :delete, data: { confirm: "Etes vous sûr ?" }%>
