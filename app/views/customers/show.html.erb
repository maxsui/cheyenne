<% content_for :title, @customer.name %>

<%= link_to 'Modifier', edit_customer_path(@customer), class: "btn btn-primary" %>
<%= link_to 'Supprimer', customer_path(@customer), class: "btn btn-default", method: :delete, data: { confirm: "Etes vous sûr ?" } %>
